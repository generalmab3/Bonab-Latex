\chapter{پیاده‌سازی و روش ارزیابی}
\label{chap:implementation}

\section{موضع این فصل}
آزمایش این فصل روی داده مصنوعی شفاف و با بذر ثابت اجرا شده است. عددها از پرونده \lr{code/results/metrics.json} آمده‌اند، نه از مقاله‌های مبنا. اسکلت پای‌تورچ همچنان مرجع پیاده‌سازی بعدی است؛ اجرای گزارش‌شده با شبکه پیش‌خور معادل در \lr{NumPy} انجام شد تا روی هر ماشین بدون وابستگی سنگین قابل بازتولید باشد.

\section{محیط نرم‌افزاری}
پیاده‌سازی پیشنهادی با پایتون و پای‌تورچ است. پای‌تورچ تعریف لایه، مشتق‌گیری خودکار و اجرا روی پردازنده مرکزی یا گرافیکی را یکجا دارد. \lr{NumPy} و \lr{Pandas} برای داده و \lr{Matplotlib} برای نمودار مناسب‌اند. ساختار پیمانه‌ای زیر توصیه می‌شود:
\begin{latin}
\begin{verbatim}
code/
|-- data.py
|-- model.py
|-- losses.py
|-- train.py
`-- evaluate.py
\end{verbatim}
\end{latin}

گزارش لاتک و کد باید در یک مخزن بمانند تا داور بتواند ادعا را با اجرا بسنجد. بذر تصادفی، نسخه کتابخانه‌ها و فرمان اجرا باید در پرونده‌ای مانند \lr{README} نوشته شود.

\section{اسکلت شبکه}
کد~\ref{lst:pytorch-model} یک شبکه پیش‌خور حداقلی است. ابعاد باید با روابط~\eqref{eq:model-input} و~\eqref{eq:model-output} یکی باشند.

\begin{latin}
\begin{lstlisting}[caption={\protect\lr{A minimal PyTorch skeleton for the energy PINN}},label={lst:pytorch-model}]
import torch
from torch import nn


class EnergyPINN(nn.Module):
    def __init__(self, n_in=6, n_out=4, width=64, depth=4):
        super().__init__()
        layers = [nn.Linear(n_in, width), nn.Tanh()]
        for _ in range(depth - 1):
            layers += [nn.Linear(width, width), nn.Tanh()]
        layers += [nn.Linear(width, n_out)]
        self.network = nn.Sequential(*layers)

    def forward(self, x):
        return self.network(x)


def upper_lower_penalty(z, lower, upper):
    above = torch.relu(z - upper).pow(2)
    below = torch.relu(lower - z).pow(2)
    return (above + below).mean()
\end{lstlisting}
\end{latin}

\section{اسکلت باقیمانده فیزیک}
کد~\ref{lst:physics-loss} همان روابط فصل قبل را به عملیات تنسوری برمی‌گرداند.

\begin{latin}
\begin{lstlisting}[caption={\protect\lr{Physics residuals for power balance and battery dynamics}},label={lst:physics-loss}]
def physics_losses(model, batch, dt, e_max, eta_ch, eta_dis):
    prediction = model(batch["x"])
    p_grid, p_ch, p_dis, soc_next = prediction.split(1, dim=1)

    power_residual = (
        p_grid + batch["p_pv"] + p_dis
        - p_ch - batch["p_load"]
    )

    expected_soc = (
        batch["soc"]
        + eta_ch * p_ch * dt / e_max
        - p_dis * dt / (eta_dis * e_max)
    )
    soc_residual = soc_next - expected_soc

    loss_power = power_residual.pow(2).mean()
    loss_soc = soc_residual.pow(2).mean()
    loss_bounds = upper_lower_penalty(
        soc_next, batch["soc_min"], batch["soc_max"]
    )
    return prediction, loss_power, loss_soc, loss_bounds
\end{lstlisting}
\end{latin}

اگر معادله پیوسته افزوده شود، ورودی زمان باید \lr{requires\_grad} داشته باشد و مشتق با \lr{torch.autograd.grad} گرفته شود. برای مدل گسسته فعلی لازم نیست.

\section{داده مصنوعی}
شبیه‌ساز \lr{code/simulate.py} بیست‌ویک شبانه‌روز ساعتی می‌سازد. بار یک الگوی روزانه با قله عصر، تولید خورشیدی فقط در روز، و قیمت خرید در ساعات اوج بالاتر است. برنامه مرجع با قاعده حریصانه ساخته می‌شود: مازاد خورشید باتری را تا سقف شارژ می‌کند، کمبود با دشارژ جبران می‌شود و باقی‌مانده از شبکه می‌آید. ظرفیت باتری $20$ کیلووات‌ساعت، بازده شارژ و دشارژ $0.95$ و حدود وضعیت شارژ $[0.20,0.90]$ است.

برای آنکه مؤلفه فیزیک کاری برای انجام داشته باشد، به برچسب‌های مجموعه آموزش نویز گاوسی با انحراف معیار $0.35$ اضافه شد. مجموعه آزمون بدون این نویز مانده است. تقسیم زمانی است: ۶۰ درصد آموزش، ۲۰ درصد اعتبارسنجی و ۲۰ درصد آزمون؛ یعنی ۱۰۲ ساعت آزمون از ۵۰۴ ساعت کل. بذر تولید داده برابر ۷ است.

قواعد اجباری آماده‌سازی:
\begin{enumerate}
  \item رد مقادیر ناممکن مانند وضعیت شارژ خارج از $[0,1]$ یا توان منفی تولید خورشیدی؛
  \item تقسیم زمانی، نه درهم‌آمیزی تصادفی نمونه‌های مجاور؛
  \item محاسبه میانگین و واریانس نرمال‌سازی فقط از آموزش؛
  \item نگهداری ترتیب زمانی برای ارزیابی چندگامی بعدی.
\end{enumerate}

\section{مدل‌های مقایسه}
حداقل دو مدل با معماری یکسان لازم است:
\begin{enumerate}
  \item شبکه داده‌محور با $\lambda_{P}=\lambda_{S}=\lambda_{c}=\lambda_{e}=0$؛
  \item شبکه فیزیک‌آگاه با رابطه~\eqref{eq:total-loss}.
\end{enumerate}
در صورت امکان، یک حل‌کننده بهینه‌سازی کلاسیک روی همان افق به‌عنوان مرجع سوم اضافه شود. مقایسه فقط وقتی منصفانه است که داده، معماری، بودجه تکرار و مجموعه آزمون یکی باشند.

\section{معیارها}
خطای پیش‌بینی:
\begin{equation}
  \mathrm{MSE}=\frac{1}{N}\sum_{i=1}^{N}
  \left\|\hat{\bm{y}}_i-\bm{y}_i\right\|_2^2.
  \label{eq:mse}
\end{equation}
باقیمانده توازن:
\begin{equation}
  E_{P}=\frac{1}{N}\sum_{i=1}^{N}|r_P(t_i)|.
  \label{eq:physics-metric}
\end{equation}
نرخ نقض قید:
\begin{equation}
  R_{v}=\frac{N_{\mathrm{violated}}}{N}\times 100.
  \label{eq:violation-rate}
\end{equation}
هزینه بهره‌برداری نیز گزارش شده است. در این نسخه یک بذر ثابت استفاده شده و پراکندگی چنداجرایی گزارش نشده است.

\section{طرح آزمایش}
\begin{enumerate}
  \item آموزش دو مدل با تقسیم یکسان؛
  \item تنظیم وزن فیزیک روی اعتبارسنجی؛
  \item ارزیابی روی آزمون ندیده؛
  \item تکرار با داده کمتر و با نویز بیشتر؛
  \item تحلیل حساسیت $\lambda_P$؛
  \item گزارش جدول واحد برای دقت، فیزیک، نقض، هزینه و زمان.
\end{enumerate}

نتایج روی مجموعه آزمون در جدول~\ref{tab:real-results} آمده است. مدل فیزیک‌آگاه خطای مربع میانگین بالاتری دارد، اما میانگین قدرمطلق باقیمانده توازن توان را از $0.082$ به $0.033$ کیلووات رسانده و نرخ نقض حدود وضعیت شارژ را از $47.1$ درصد به $21.6$ درصد کاهش داده است. هزینه متوسط بهره‌برداری اندکی کمتر شده است.

\begin{table}[H]
  \centering
  \caption{نتایج آزمون روی داده مصنوعی؛ بذر ۷، شبکه پیش‌خور ۴۸ نورونی}
  \label{tab:real-results}
  \begin{tabular}{lcccc}
    \toprule
    مدل & \lr{MSE} & باقیمانده توان & نقض قید (درصد) & هزینه \\
    \midrule
    شبکه داده‌محور & 0.0099 & 0.082 & 47.1 & 0.304 \\
    شبکه فیزیک‌آگاه & 0.0167 & 0.033 & 21.6 & 0.301 \\
    \bottomrule
  \end{tabular}
\end{table}

شکل~\ref{fig:soc-day} یک شبانه‌روز نمونه و شکل~\ref{fig:power-residual} باقیمانده توازن را نشان می‌دهد.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\textwidth]{fig-soc-day.png}
  \caption{بار و تولید خورشیدی در یک روز نمونه، به‌همراه وضعیت شارژ مرجع و دو مدل}
  \label{fig:soc-day}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\textwidth]{fig-power-residual.png}
  \caption{قدر مطلق باقیمانده توازن توان روی ساعات آزمون}
  \label{fig:power-residual}
\end{figure}

\section{خوانش محدود نتایج}
این آزمایش یک ریزشبکه مصنوعی تک‌سایت است و برتری را در جهان واقعی ثابت نمی‌کند. قیدها همچنان نرم‌اند؛ به همین دلیل نقض حدود صفر نشده است. معماری از تنظیم پیشنهادی جدول~\ref{tab:initial-settings} کوچک‌تر گرفته شد تا آموزش روی پردازنده معمولی در چند ثانیه تمام شود. برای بازتولید:

\begin{latin}
\begin{verbatim}
python code/simulate.py
\end{verbatim}
\end{latin}

\section{آمادگی برای دفاع از همین فصل}
داور می‌پرسد داده واقعی است یا مصنوعی. پاسخ دقیق: مصنوعی، با قاعده صریح و بذر ۷. می‌پرسد چرا \lr{MSE} فیزیک‌آگاه بدتر است. پاسخ: برچسب آموزش نویزی است و وزن فیزیک برازش آن نویز را فدا می‌کند تا توازن توان بهتر شود. می‌پرسد کد کجاست: پوشه \lr{code}.
